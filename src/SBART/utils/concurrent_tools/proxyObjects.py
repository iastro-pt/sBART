from multiprocessing.managers import BaseManager


class DataClassManager(BaseManager):
    """This will be the server that manages the shared objects

    Parameters
    ----------
    BaseManager : [type]
        [description]
    """

    pass
